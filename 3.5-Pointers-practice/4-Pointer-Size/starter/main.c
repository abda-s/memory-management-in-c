#include <stdio.h>

int main() {
    // TODO: Write a program to print the size (in bytes) of a pointer to an integer, 
    // a pointer to a character, and a pointer to a float on your system.
    
    return 0;
}
