#include <stdio.h>

int main() {
    int num = 100;
    // TODO: Write a program that uses the dereference operator (*) 
    // to change the value of a variable through its pointer.
    
    return 0;
}
