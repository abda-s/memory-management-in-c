#include <stdio.h>

int main() {
    int arr[5] = {1, 2, 3, 4, 5};
    int other_var = 10;
    // TODO: Attempt to assign a new address to the array's name. 
    // Observe the compilation error.
    
    return 0;
}
